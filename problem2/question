2. Find Top N Frequent Errors in Logs
Problem:
Given a list of log messages, find the top N most frequent error messages.

Example Input:


logs = [
    "Database connection failed",
    "Service crashed at endpoint /api/v1/users",
    "Database connection failed",
    "Memory leak detected",
    "Service crashed at endpoint /api/v1/users",
    "Service crashed at endpoint /api/v1/users",
]


Example Output for N=2:


[("Service crashed at endpoint /api/v1/users", 3), ("Database connection failed", 2)]
